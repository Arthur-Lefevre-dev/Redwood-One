services:

  postgres:
    image: postgres:16-alpine
    container_name: redwood_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB:       ${POSTGRES_DB:-redwood}
      POSTGRES_USER:     ${POSTGRES_USER:-redwood}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-redwood}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - redwood_net

  redis:
    image: redis:7-alpine
    container_name: redwood_redis
    restart: unless-stopped
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - redwood_net

  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile.api
    container_name: redwood_api
    restart: unless-stopped
    env_file: .env
    volumes:
      - tmp_data:/tmp/redwood
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - redwood_net

  worker:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    container_name: redwood_worker
    restart: unless-stopped
    env_file: .env
    volumes:
      - tmp_data:/tmp/redwood
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - redwood_net
    # GPU AMD (Linux hôte) : exposer le nœud render pour ffmpeg VAAPI/AMF + détection lspci/vainfo
    # devices:
    #   - /dev/dri:/dev/dri
    # group_add:
    #   - video
    # Sur VPS avec NVIDIA + nvidia-container-toolkit, décommenter :
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]

  beat:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    container_name: redwood_beat
    restart: unless-stopped
    env_file: .env
    command: celery -A worker.tasks beat --loglevel=info
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - redwood_net

  flower:
    build:
      context: ..
      dockerfile: docker/Dockerfile.worker
    container_name: redwood_flower
    restart: unless-stopped
    env_file: .env
    command: celery -A worker.tasks flower --port=5555 --basic_auth=${FLOWER_USER}:${FLOWER_PASSWORD}
    ports:
      - "5555:5555"
    depends_on:
      - redis
      - worker
    networks:
      - redwood_net

  nginx:
    image: nginx:alpine
    container_name: redwood_nginx
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ../nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ../frontend:/usr/share/nginx/html:ro
      - ../nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - api
    networks:
      - redwood_net

volumes:
  postgres_data:
  redis_data:
  tmp_data:

networks:
  redwood_net:
    driver: bridge
